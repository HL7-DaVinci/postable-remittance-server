# tells Lombok that this is the root directory
config.stopBubbling = true
# tells Lombok to add @lombok.Generated annotation to all generated methods, which are ignored in coverage report
lombok.addLombokGeneratedAnnotation = true